# Core data stack
pandas>=2.0
numpy>=1.24
openpyxl>=3.1          # .xlsx
xlrd>=2.0             # legacy .xls
pyarrow>=12.0          # .parquet
PyYAML>=6.0

# Machine learning
scikit-learn>=1.3
scipy>=1.10

# PDF rule extraction
pdfplumber>=0.10
PyMuPDF>=1.23          # fitz fallback

# Dashboard / visualization
streamlit>=1.30
plotly>=5.18
matplotlib>=3.7

# Reporting
jinja2>=3.1
XlsxWriter>=3.1

# Optional dimensionality reduction for visualization only
umap-learn>=0.5 ; python_version < "3.13"

# Testing
pytest>=8.0

# Optional OCR fallback for scanned regulatory PDFs (NOT required to run the app).
# Needs the Tesseract engine + Vietnamese language pack installed on the OS.
#   pytesseract>=0.3
#   pdf2image>=1.17
#   Pillow>=10.0
